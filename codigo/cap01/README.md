# Código del capítulo 1

Capítulo conceptual: por ahora sin experimentos (pseudocódigo en la prosa).

## Dependencias
python>=3.10  (sin más, de momento)

## Scripts
- _(aún ninguno)_ — al añadir uno, documentar aquí qué mide, el comando exacto
  de ejecución y las cifras que registra en `cifras.csv` (vía `comun/registro`).
  Las salidas (PNG/TXT que incluya el `.tex`, resultados costosos) van a
  `salidas/`.
